/** @type {import('next').NextConfig} */
const nextConfig = {
    images: 
        {
            domains: ['xj5fixoja8onpyyj.public.blob.vercel-storage.com'],
        },
    };

export default nextConfig;
